# Build progress

Short-lived notes for **maintainers** on what is in flight. Trim or archive rows when work lands on `main`.

| Area          | Status | Notes                                      |
| ------------- | ------ | ------------------------------------------ |
| CLI / harness | Active | See `AGENTS.md` and `npm run verify` gate. |

For release history, use git tags and the changelog section of `README.md` when present.
