# Architectural decisions

Lightweight log of notable choices for **skissue** and this repository. Expand entries when a decision affects agents, CI, or consumers.

| Date       | Decision                                                                 |
| ---------- | ------------------------------------------------------------------------ |
| (template) | Prefer versioned docs (`docs/`, `AGENTS.md`) over chat-only conventions. |

When a decision is superseded, add a row rather than deleting history.
