# create-pull-request-workflow

Soft skill: validate PR title (`check:pr-title`), push the branch, then `gh pr create --draft` or `gh pr edit` using the repo PR template. Requires [GitHub CLI](https://cli.github.com/) (`gh`) installed and authenticated.

See [SKILL.md](SKILL.md) for the full step-by-step workflow.
